<Project>
  <!-- Import parent Directory.Build.props -->
  <Import Project="$([MSBuild]::GetPathOfFileAbove('Directory.Build.props', '$(MSBuildThisFileDirectory)../'))" />

  <!-- Domain-specific configuration for Performance mods -->
  <PropertyGroup>
    <ModIdPrefix>Performance</ModIdPrefix>
  </PropertyGroup>
</Project>
