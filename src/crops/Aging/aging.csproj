<Project Sdk="Microsoft.NET.Sdk">
  <!-- Inherits from root Directory.Build.props -->
  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <RootNamespace>Ehm93.VS.Crops.Aging</RootNamespace>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\Common\common.csproj">
      <!-- Compile-only: cropscommon is a separate mod (declared in modinfo deps); do not bundle its dll. -->
      <Private>false</Private>
    </ProjectReference>
  </ItemGroup>
</Project>
